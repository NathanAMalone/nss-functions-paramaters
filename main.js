// define parameter; divide by 4; return the number; save to var; console.log; 
const quarterValue = (number) => {
    const quarter = number/4
    return quarter
}
// Invoke the function and store the return value
const numFourth = quarterValue(10)

// Log the return value
console.log(numFourth)